import React from 'react';
import { expect } from 'chai';
import { shallow } from 'enzyme';
import MarkdownRichEditor from '.';

describe('<MarkdownRichEditor />', () => {
  /* Recommended test-cases

  it('should have default props', () => {
    let component = <MarkdownRichEditor />;
    expect(component.props.testProp).to.equal('Give me back my label!');
    expect(component.props.onClick).to.be.a('function');
  });
  it('should have the right class name', () => {
    let wrapper = shallow(<MarkdownRichEditor className="test-class-name" />);
    expect(wrapper).to.have.className('markdown-rich-editor');
    expect(wrapper).to.have.className('test-class-name');
  });

  */
});
